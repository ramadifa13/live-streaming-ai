import { synthesizeSpeech } from "./src/services/tts.js"; import fs from "fs"; async function run() { console.log("Generating..."); const res = await synthesizeSpeech({ text: "Halo kak, selamat datang di live streaming. Jangan lupa checkout ya karena produk ini bagus banget.", voice: "id-ID-GadisNeural" }); if (res.audioBuffer) { fs.writeFileSync("test.wav", res.audioBuffer); console.log("Saved test.wav"); } } run();
